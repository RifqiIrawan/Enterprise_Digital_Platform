# Build-time only (Vite bakes VITE_-prefixed vars into the JS bundle) -- not
# read at container runtime, so changing this requires rebuilding the
# frontend image, not just restarting the container.
VITE_API_BASE_URL=https://api.REPLACE_ME_DOMAIN
