PORT=8094
DATABASE_URL=postgres://platform:REPLACE_ME_DB_PASSWORD@REPLACE_ME_DB_HOST:5432/iot_service?sslmode=require
KAFKA_BROKERS=REPLACE_ME_KAFKA_BROKERS
MQTT_BROKER_URL=REPLACE_ME_MQTT_BROKER_URL
# Simulator generates fake device readings -- off in staging/prod, where
# real devices should be publishing instead. See internal/config/config.go.
IOT_SIMULATOR_ENABLED=false
