apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  labels:
    app: crm-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-service
  template:
    metadata:
      labels:
        app: crm-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8096"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: crm-service
          image: REPLACE_ME_REGISTRY/crm-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8096
          envFrom:
            - configMapRef:
                name: crm-service-config
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8096
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8096
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: crm-service
  labels:
    app: crm-service
spec:
  selector:
    app: crm-service
  ports:
    - port: 8096
      targetPort: 8096
