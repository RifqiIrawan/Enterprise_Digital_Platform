apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot-service
  labels:
    app: iot-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iot-service
  template:
    metadata:
      labels:
        app: iot-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8094"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: iot-service
          image: REPLACE_ME_REGISTRY/iot-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8094
          envFrom:
            - configMapRef:
                name: iot-service-config
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8094
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8094
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: iot-service
  labels:
    app: iot-service
spec:
  selector:
    app: iot-service
  ports:
    - port: 8094
      targetPort: 8094
