# Kubernetes Base Manifests

Placeholder untuk manifest dasar (Deployment, Service, ConfigMap, Secret) tiap service.

Akan diisi sesuai `14_Kubernetes_Deployment.md` pada fase implementasi K8s (setelah service stabil berjalan via docker-compose).
