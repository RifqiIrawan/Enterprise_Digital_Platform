apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-service
  labels:
    app: production-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: production-service
  template:
    metadata:
      labels:
        app: production-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: production-service
          image: REPLACE_ME_REGISTRY/production-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8090
          envFrom:
            - configMapRef:
                name: production-service-config
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: production-service
  labels:
    app: production-service
spec:
  selector:
    app: production-service
  ports:
    - port: 8090
      targetPort: 8090
