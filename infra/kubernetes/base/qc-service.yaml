apiVersion: apps/v1
kind: Deployment
metadata:
  name: qc-service
  labels:
    app: qc-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qc-service
  template:
    metadata:
      labels:
        app: qc-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8091"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: qc-service
          image: REPLACE_ME_REGISTRY/qc-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8091
          envFrom:
            - configMapRef:
                name: qc-service-config
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: qc-service
  labels:
    app: qc-service
spec:
  selector:
    app: qc-service
  ports:
    - port: 8091
      targetPort: 8091
