# Sales Service (Planned)

Status: **Fase 2 — belum di-scaffold.**

Role terkait: `Sales`.

Lingkup awal (indikatif): customer, sales order, quotation — scoped per `company_id` / `branch_id`.

Akan mengikuti struktur yang sama dengan service di `../../services/` dan publish event Kafka `sales.*`.
